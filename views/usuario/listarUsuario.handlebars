<form method="get" action="/listarUsuario" class="mb-3 d-flex" style="gap: 8px;">
    <input type="text" name="nome" class="form-control" placeholder="Buscar por nome..." value="{{filtroNome}}">
    <button type="submit" class="btn btn-primary btn-sm">Buscar</button>
    <a href="/listarUsuario" class="btn btn-secondary btn-sm">Limpar</a>
</form>

<table class="table">
    <thead>
        <tr>
            <th scope="col">Nome</th>
            <th scope="col">Login</th>
            <th scope="col">Senha</th>
            <th scope="col">Tipo</th>
            <th scope="col">Editar</th>
            <th scope="col">Deletar</th>
        </tr>
    </thead>
    <tbody>
        {{#each usuarios}}
        <tr>
            <th scope="row">{{nome}}</th>
            <th scope="row">{{login}}</th>
            <td>{{senha}}</td>
            <td>{{tipo}}</td>
            <td><a href="editarUsuario/{{id}}">Editar</a></td>
            <td><a href="excluirUsuario/{{id}}">Deletar</a></td>
        </tr>
        {{/each}}
    </tbody>
</table>

<div class="mt-3 d-flex justify-content-between">
    <a href="/listarUsuario?pagina={{subtract paginaAtual 1}}" class="btn btn-outline-secondary btn-sm">Anterior</a>
    <a href="/listarUsuario?pagina={{add paginaAtual 1}}" class="btn btn-outline-secondary btn-sm">Próxima</a>
</div>